# LiveKit Egress — запись Room Composite → MinIO
log_level: info
api_key: devkey
api_secret: secret
ws_url: ws://livekit:7880
insecure: true
health_port: 8080
redis:
  address: redis:6379
s3:
  access_key: evoroom
  secret: evoroomsecret
  endpoint: http://minio:9000
  bucket: evoroom
  region: us-east-1
  force_path_style: true
